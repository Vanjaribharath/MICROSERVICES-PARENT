apiVersion: v1
kind: Service
metadata:
  name: bff-gateway
  namespace: microservices-training
spec:
  selector: { app: bff-gateway }
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
